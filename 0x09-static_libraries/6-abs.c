#include "main.h"
/**
* _abs - computes the absolute value of an integer
* @t: integer to be computed
* Return: t for t greater than zero, -t for t less than zero
*/
int _abs(int t)
{
if (t > 0)
	return (t);
else
	return (-t);
}
