#include <stdio.h>
/**
 * main - print alphabet
 * description: print alphabet in order
 * Return: Always success
 */
int main(void)
{
int num;
for (num = 0; num < 10; num++)
printf("%d", num);
putchar('\n');
return (0);
}
